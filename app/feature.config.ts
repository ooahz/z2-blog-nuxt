const featureConfig = {
    // 评论功能（enable,disable)
    comment: "enable",
    // 搜索功能（enable,disable)
    search: "enable",
    // 友链交换功能（enable,disable)
    friendLink: "enable",
}

export default featureConfig;
